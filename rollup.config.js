// This project uses TypeScript compilation directly (tsc)
// The compiled output in dist/better-curtain-card.js is ready for Home Assistant

module.exports = {
  // Rollup configuration is no longer needed
  // Use 'npm run build' which compiles TypeScript directly
};